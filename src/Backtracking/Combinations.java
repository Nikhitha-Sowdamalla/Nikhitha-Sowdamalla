package Backtracking;

import java.util.ArrayList;
//import java.util.LinkedList;
import java.util.List;

public class Combinations {
//    public List<List<Integer>> combine(int n, int k) {
//        List<List<Integer>> result = new ArrayList<>();
//        List<Integer> current = new LinkedList<>();
//        generateCombinations(n, k, 1, current, result);  // Start from 1
//        return result;
//    }
//
//    private void generateCombinations(int n, int k, int start, List<Integer> current, List<List<Integer>> result) {
//        if (current.size() == k) {
//            result.add(new LinkedList<>(current));  // Add the current combination to result
//            return;
//        }
//
//        for (int i = start; i <= n ; i++) {
//            current.add(candidates[i]);  // Choose the current number
//            generateCombinations(n, k, i + 1, current, result);  // Recur for the next number
//            current.removeLast(); // Backtrack, remove the last added number
//        }
//    }

}
